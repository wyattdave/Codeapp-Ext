/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import type { CdsDatabaseReference, ConnectionReference } from './AppMetadata.js';
export interface PowerConfig {
    /** The fully-qualified URL where the app is hosted. */
    appUrl: string;
    /** Unique application identifier. */
    appId: string;
    /** Human-readable application name. */
    appName: string;
    /** Environment identifier the app belongs to. */
    environmentName: string;
    /** Tenant identifier. */
    tenantId: string;
    /** Additional configuration properties. */
    properties?: Record<string, unknown>;
    /** Human-readable display name for the app. */
    appDisplayName?: string;
    /** App description. */
    description?: string | null;
    /** Environment unique identifier. */
    environmentId?: string;
    /** Path to the build output directory. */
    buildPath?: string;
    /** Entry point within the build output. */
    buildEntryPoint?: string;
    /** Path to the app logo. */
    logoPath?: string;
    /** Local URL for development. */
    localAppUrl?: string;
    /** Deployment region. */
    region?: string;
    /** Connection references declared in the config. */
    connectionReferences?: Record<string, ConnectionReference>;
    /** CDS database references declared in the config. */
    databaseReferences?: Record<string, CdsDatabaseReference>;
    /** App version string. */
    version?: string;
}
//# sourceMappingURL=PowerConfig.d.ts.map